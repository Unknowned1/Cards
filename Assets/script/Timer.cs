using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Timer : MonoBehaviour
{
    /*public Text player_timer_txt, enemy_timer_txt;
    string player_timer_str, enemy_timer_str;

    int player_timer_value, enemy_timer_value;

    // Start is called before the first frame update
    void Start()
    {
        player_timer_value = 10;
        enemy_timer_value = 10;

        player_timer_txt = GetComponent<Text>();
        player_timer_str = GetComponent<Text>().text;

        enemy_timer_txt = GetComponent<Text>();
        enemy_timer_str = GetComponent<Text>().text;

    }

    // Update is called once per frame
    void Update()
    {
        UpdateTimer(); 
    }

    public void UpdateTimer() {
        Debug.Log("Updating timers ...");
    }
    */
}
